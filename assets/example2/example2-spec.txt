需求項目:
1. UI介面開啟前讓我手動輸入工程名稱，此案例為"埔里新家新建工程"，並顯示於輸出料單的左上角工程名稱。

2. 新增各類預設值設定。

  a. 角度: 隨讀取文字不同填入角度至圖示中。  

  b. 副筋: 隨讀取文字不同填入長度至圖示中。

  c. 主筋: 隨讀取文字不同填入長度至圖示中。但此由於又區分為3大類，但每一個案子只會對應到一類，所以此項目要在UI做下拉選項3種表格給我做切換。

3. 隨讀取文字不同填入文字說明至圖示中。

4. 隨讀取文字不同填入文字說明至料單的"備註"欄位中。

5. 此3張料表命名皆採用範例1規範執行，圖示順序也由大號數到小號數，但細部的每一種鋼筋種類我還尚未決定排序，我近期會將此補上。

6. 補充範例1，排序條件為: 號數> 形狀> 總長度。

7. 預設值主筋部分可能比較複雜怕你沒有理解所以我CAD裡面沒有給範例，後續可以補上。